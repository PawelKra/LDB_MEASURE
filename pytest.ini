[pytest]
testpaths = testy
# every warning is a failure - catches resource leaks, numpy RuntimeWarnings,
# and PyQt6 / matplotlib / numpy deprecations before they become bugs
filterwarnings =
    error
